import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'constrole.inspecao',
  appName: 'controlInspection',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
